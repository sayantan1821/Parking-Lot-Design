package repositories;

import models.Gate;

import java.util.Optional;

public class GateRepository {
    public Optional<Gate> findGetById(long getId) {
        // get data from db;
        return Optional.empty();
    }
}
